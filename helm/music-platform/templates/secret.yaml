{{- if not .Values.db.existingSecret }}
{{- $dbHost := include "music-platform.dbServiceName" . -}}
{{- $secretName := include "music-platform.secretName" . -}}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- $postgresPassword := "" -}}
{{- if and $existingSecret (hasKey $existingSecret.data "POSTGRES_PASSWORD") -}}
{{- $postgresPassword = (index $existingSecret.data "POSTGRES_PASSWORD" | b64dec) -}}
{{- else if .Values.db.password -}}
{{- $postgresPassword = .Values.db.password -}}
{{- else -}}
{{- $postgresPassword = printf "%s!%s#%s" (randAlphaNum 13) (randAlphaNum 13) (randAlphaNum 12) -}}
{{- end -}}
{{- if eq (trim $postgresPassword) "" -}}
{{- fail "POSTGRES_PASSWORD must be non-empty. Set db.password or provide existingSecret with POSTGRES_PASSWORD." -}}
{{- end -}}
{{- $postgresPasswordForUrl := urlquery $postgresPassword -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "music-platform.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ $postgresPassword | quote }}
  DATABASE_URL: {{ printf "postgresql+psycopg://%s:%s@%s:%v/%s" .Values.db.user $postgresPasswordForUrl $dbHost .Values.db.service.port .Values.db.name | quote }}
{{- end }}
